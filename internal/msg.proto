syntax = "proto3";
package msg;

// request is client to server ;
message request {
    string method = 1;
    bytes params = 2;
    uint64 id=3;
}

// response is server to client ;
message response {
    uint64 id = 1;
    bytes result = 2;
    string Error = 3;
}